import * as Styled from './styles';

const Loading = (props) => {
  return (
    <Styled.Loading>
      <Styled.Loader src="../images/icons/loader.svg" />
    </Styled.Loading>
  );
};

export default Loading;
